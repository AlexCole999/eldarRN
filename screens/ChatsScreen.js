import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet, ScrollView } from 'react-native';

const ChatsScreen = () => {

  return (
    <View style={{ paddingTop: 400, alignItems: 'center' }}>
      <Text>Чаты в тестовом режиме</Text>
    </View>
  );
};


export default ChatsScreen;
